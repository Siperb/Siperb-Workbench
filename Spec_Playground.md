# Playground
- Allows user to write code in a code editor that has intelisense as if we where in the browser inspect console log.
- phone. will display all the functions available.
- Users can press a button 'Run' that will take the code editor text content and act as if its a test.
- Save button that will open in the ./tests folder where a user can save their test.
- I still want to see the 'test' logs

